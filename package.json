{
    "name": "pinefetch",
    "version": "1.6.1",
    "scripts": {
        "dev": "tauri dev",
        "prepare:whisper-runtime": "bash scripts/prepare-whisper-runtime.sh",
        "prepare:ffmpeg-runtime": "bash scripts/prepare-ffmpeg-runtime.sh",
        "prepare:deno-runtime": "bash scripts/prepare-deno-runtime.sh",
        "prepare:windows-runtime": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/prepare-windows-runtime.ps1",
        "build": "npm run prepare:whisper-runtime && npm run prepare:ffmpeg-runtime && npm run prepare:deno-runtime && tauri build",
        "build:windows": "npm run prepare:windows-runtime && tauri build --target x86_64-pc-windows-msvc",
        "build:adhoc-dmg": "bash -lc 'set -euo pipefail; npm run build -- --bundles app; APP=\"src-tauri/target/release/bundle/macos/PineFetch.app\"; if [ ! -d \"$APP\" ]; then echo \"App not found at $APP\"; exit 1; fi; codesign --force --deep --sign - \"$APP\"; codesign --verify --deep --strict --verbose=2 \"$APP\"; mkdir -p src-tauri/target/release/bundle/dmg; VERSION=$(node -p \"require(\\\"./src-tauri/tauri.conf.json\\\").package.version\"); OUT=\"src-tauri/target/release/bundle/dmg/PineFetch_${VERSION}_aarch64_adhoc.dmg\"; hdiutil create -volname \"PineFetch\" -srcfolder \"$APP\" -ov -format UDZO \"$OUT\"; echo \"Created $OUT\";'",
        "publish": "sh publish.sh"
    },
    "devDependencies": {
        "@tauri-apps/cli": "^1.5.0"
    },
    "contributors": [
        {
            "name": "Oliver Jessner",
            "url": "https://github.com/oliverjessner",
            "hireable": true
        }
    ],
    "dependencies": {
        "pinefetch-designsystem": "^0.1.1"
    }
}
